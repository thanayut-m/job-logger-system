import Buttons from "../../../../components/MUI/Buttons"

const Footer_ResetPassword = ({
    onClick
}) => {
    return (
        <Buttons
            variant="contained"
            onClick={onClick}
        >
            บันทึก
        </Buttons>
    )
}
export default Footer_ResetPassword